# -*- coding: utf-8 -*-
"""Readable self-contained HTML template for the Mae Ping weather dashboard."""

DASHBOARD_V2_HTML = r'''<!doctype html>
<html lang="th">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<title>สภาพอากาศอุทยานแห่งชาติแม่ปิง</title>
<style>
:root{--ink:#173126;--muted:#687970;--paper:#f4f6f1;--card:#fff;--line:#dce4dd;--forest:#176045;--rain:#167db0;--heat:#d7673f;--violet:#755caa;--warn:#a76715;--low:#9a453d;--shadow:0 8px 24px #203c2a12}
*{box-sizing:border-box}body{margin:0;background:var(--paper);color:var(--ink);font-family:"Noto Sans Thai",system-ui,sans-serif}.shell{max-width:1280px;margin:auto;padding:20px}.top{display:flex;justify-content:space-between;gap:20px;align-items:flex-start}.eyebrow{font-size:.72rem;font-weight:800;letter-spacing:.08em;color:var(--forest)}h1{font-size:clamp(1.7rem,3vw,2.6rem);line-height:1.15;margin:.25rem 0}.subtitle,.muted{color:var(--muted)}.freshbox{text-align:right}.fresh{display:inline-block;background:#e5f1e9;color:#205f43;padding:8px 11px;border-radius:999px;font-size:.82rem;font-weight:750;white-space:nowrap}.fresh.warning{background:#fff0d4;color:#8a5a12}.fresh.stale{background:#f9dfdc;color:var(--low)}.freshdetail{margin-top:6px;color:var(--muted);font-size:.72rem;white-space:nowrap}.controls{display:grid;grid-template-columns:auto minmax(230px,420px) 1fr;gap:10px;align-items:center;margin:18px 0}.tabs{display:flex;background:#e8eee9;padding:4px;border-radius:12px}.tabs button,.select,button{font:inherit}.tabs button{border:0;background:transparent;padding:9px 13px;border-radius:9px;cursor:pointer}.tabs button.active{background:var(--forest);color:#fff}.select{width:100%;border:1px solid var(--line);background:#fff;padding:11px 13px;border-radius:12px;color:var(--ink)}.statusline{text-align:right;font-size:.84rem;color:var(--muted)}.kpis{display:grid;grid-template-columns:repeat(5,1fr);gap:12px;margin-bottom:12px}.card{background:var(--card);border:1px solid var(--line);border-radius:16px;box-shadow:var(--shadow)}.kpi{padding:15px}.kpi span{display:block;color:var(--muted);font-size:.78rem}.kpi b{display:block;font-size:1.65rem;margin-top:4px}.main{display:grid;grid-template-columns:minmax(0,1.45fr) minmax(310px,.75fr);gap:12px}.mapcard,.detail,.chartcard,.sites,.advanced{padding:15px}.maphead{display:flex;justify-content:space-between;gap:16px;align-items:flex-end;margin-bottom:10px}.maphead h2,.detail h2,.sectionhead h2{margin:0;font-size:1.08rem}.legend{font-size:.76rem;color:var(--muted)}.maplegend{min-width:215px}.legendbar{height:8px;border-radius:999px;background:linear-gradient(90deg,#8ba58f,#84cad5,#36a1bd,#176ea0,#60439a);box-shadow:inset 0 0 0 1px #ffffffaa;margin-bottom:4px}.legendlabels{display:flex;justify-content:space-between;font-size:.68rem;color:var(--muted)}.mapwrap{position:relative;height:520px;background:radial-gradient(circle at 23% 17%,#ffffff99 0,transparent 28%),linear-gradient(155deg,#dcebe2 0,#cbded2 43%,#b9d0c1 100%);border:1px solid #c8d8ce;border-radius:16px;overflow:hidden;box-shadow:inset 0 0 55px #315d4330}.mapwrap:after{content:"ขอบเขต OSM ชั่วคราว";position:absolute;right:12px;bottom:10px;padding:5px 8px;border-radius:8px;background:#f9fcf8dd;color:#5c7467;font-size:.67rem;backdrop-filter:blur(5px)}.map{width:100%;height:100%}.mapgrain{opacity:.42}.mapgrid{stroke:#fff;stroke-width:.8;stroke-dasharray:3 6;opacity:.38}.parkshadow{fill:#1d493a30;filter:url(#mapShadow);transform:translate(3px,5px)}.park{fill:url(#parkFill);stroke:#174f3b;stroke-width:2.4;vector-effect:non-scaling-stroke}.parkinner{fill:url(#terrainPattern);stroke:#ffffff99;stroke-width:.8;vector-effect:non-scaling-stroke}.markerhalo{fill:none;stroke:#fff;stroke-width:7;opacity:.35}.marker{stroke:#fff;stroke-width:2.5;cursor:pointer;filter:url(#markerShadow);transition:r .15s ease,filter .15s ease}.marker:hover{filter:url(#markerShadowStrong)}.marker.selected{stroke:#143c2e;stroke-width:4}.selectedring{fill:none;stroke:#173126;stroke-width:1.5;stroke-dasharray:3 3;opacity:.8}.maplabelbox{fill:#ffffffec;stroke:#d6e2da;stroke-width:.8;filter:url(#labelShadow)}.maplabel{font-size:10.5px;font-weight:800;fill:#173126}.mapvalue{font-size:9px;font-weight:700;fill:#60736a}.compass{fill:#244f3e}.compasstext,.scaletext{font-size:9px;font-weight:800;fill:#355f4d}.scaleline{stroke:#355f4d;stroke-width:2}.side{display:grid;gap:12px;align-content:start}.placekind{color:var(--forest);font-size:.76rem;font-weight:800}.detail h2{font-size:1.35rem;margin-top:4px}.readings{display:grid;grid-template-columns:1fr 1fr 1fr;gap:9px;margin:14px 0}.reading{padding:13px;background:#f2f6f1;border-radius:12px}.reading span{display:block;color:var(--muted);font-size:.76rem}.reading b{font-size:1.5rem}.range{font-size:.76rem;color:var(--muted);margin-top:3px}.qualitybox{border-top:1px solid var(--line);padding-top:12px}.qualityrow{display:flex;justify-content:space-between;gap:8px;align-items:center}.pill{display:inline-block;border-radius:999px;padding:5px 8px;font-size:.74rem;font-weight:750;background:#e5f1e9;color:#226044}.pill.medium{background:#fff0d4;color:#8a5a12}.pill.low{background:#f9dfdc;color:var(--low)}.source{font-size:.82rem;color:var(--muted);margin:8px 0 0}.explain{font-size:.8rem;color:var(--muted);line-height:1.55}.chartcard,.sites,.advanced{grid-column:1/-1}.sectionhead{display:flex;justify-content:space-between;align-items:baseline;gap:12px;margin-bottom:10px}.chart{width:100%;height:240px;background:#fafcf9;border-radius:11px}.axis{stroke:#bbc8bd}.gridline{stroke:#e4ebe5}.forecastline{fill:none;stroke:var(--violet);stroke-width:2.5}.point{fill:var(--forest);stroke:#fff;stroke-width:2}.charttext{fill:#748179;font-size:10px}.sitegrid{display:grid;grid-template-columns:repeat(3,1fr);gap:9px}.siteitem{border:1px solid var(--line);background:#fff;padding:11px;border-radius:12px;text-align:left;cursor:pointer;color:var(--ink)}.siteitem:hover,.siteitem.selected{border-color:var(--forest);box-shadow:0 0 0 2px #17604518}.sitename{font-weight:800}.sitevals{display:flex;flex-wrap:wrap;gap:6px 12px;margin-top:5px;font-size:.85rem}.sitehint{font-size:.73rem;color:var(--muted);margin-top:5px}.advanced{box-shadow:none}.advanced summary{cursor:pointer;font-weight:800}.advancedbody{margin-top:12px;display:grid;grid-template-columns:1fr 1fr;gap:14px}.advanced h3{font-size:.95rem;margin:.3rem 0}.advanced ul{padding-left:20px;margin-top:6px;color:var(--muted);font-size:.82rem}.source-links a{color:var(--forest)}.foot{margin:12px 0 0;text-align:center;color:var(--muted);font-size:.75rem}
@media(max-width:900px){.controls{grid-template-columns:1fr 1fr}.statusline{grid-column:1/-1;text-align:left}.kpis{grid-template-columns:1fr 1fr}.main{grid-template-columns:1fr}.mapwrap{height:460px}.sitegrid{grid-template-columns:1fr 1fr}}
@media(max-width:560px){.shell{padding:12px}.top{display:block}.freshbox{text-align:left;margin-top:8px}.freshdetail{white-space:normal}.controls{grid-template-columns:1fr}.statusline{text-align:left;grid-column:auto}.kpis{grid-template-columns:1fr 1fr}.kpi b{font-size:1.35rem}.mapwrap{height:390px}.sitegrid{grid-template-columns:1fr}.advancedbody{grid-template-columns:1fr}.readings{grid-template-columns:1fr 1fr}}
</style>
</head>
<body><main class="shell">
<header class="top"><div><div class="eyebrow">อุทยานแห่งชาติแม่ปิง</div><h1>ฝนและอุณหภูมิรายพื้นที่</h1><div class="subtitle">ค่าตรวจวัดและค่าประเมินแบบผสม แสดงแหล่งที่มาและความไม่แน่นอนอย่างตรงไปตรงมา</div></div><div class="freshbox"><div class="fresh" id="freshBadge">กำลังอ่านข้อมูลล่าสุด</div><div class="freshdetail" id="generatedBadge">—</div></div></header>
<section class="controls" aria-label="เลือกข้อมูล"><div class="tabs"><button id="rainBtn" class="active">ฝน 3 ชม.</button><button id="rain24Btn">ฝน 24 ชม.</button><button id="tempBtn">อุณหภูมิ</button></div><select id="siteSelect" class="select" aria-label="เลือกสถานที่"></select><div class="statusline" id="updateLine">—</div></section>
<section class="kpis"><div class="card kpi"><span>พื้นที่ที่มีค่าประเมิน</span><b id="kpiCoverage">—</b></div><div class="card kpi"><span>ฝนสูงสุด 3 ชั่วโมง</span><b id="kpiRain">—</b></div><div class="card kpi"><span>ฝนสูงสุด 24 ชั่วโมง</span><b id="kpiRain24">—</b></div><div class="card kpi"><span>ช่วงอุณหภูมิ</span><b id="kpiTemp">—</b></div><div class="card kpi"><span>จุดความเชื่อมั่นปานกลางขึ้นไป</span><b id="kpiConfidence">—</b></div></section>
<section class="main">
<article class="card mapcard"><div class="maphead"><div><h2>แผนที่จุดรายงาน</h2><div class="muted" style="font-size:.8rem">สีและขนาดวงกลมแสดงค่าประเมินล่าสุด · กดจุดเพื่อดูรายละเอียด</div></div><div class="maplegend"><div class="legendbar" id="legendBar"></div><div class="legendlabels"><span id="legendLow">น้อย</span><span id="legendMetric">ฝนสะสม 3 ชม.</span><span id="legendHigh">มาก</span></div></div></div><div class="mapwrap"><svg id="map" class="map" viewBox="0 0 760 520" role="img" aria-label="แผนที่ค่าประเมินฝนและอุณหภูมิในอุทยานแห่งชาติแม่ปิง"></svg></div></article>
<aside class="side"><section class="card detail"><div class="placekind" id="detailKind">จุดรายงาน</div><h2 id="detailName">เลือกสถานที่</h2><div class="muted" id="detailTime">—</div><div class="readings"><div class="reading"><span>ฝนสะสม 3 ชม.</span><b id="detailRain">—</b><div class="range" id="detailRainRange">—</div></div><div class="reading"><span>ฝนสะสม 24 ชม.</span><b id="detailRain24">—</b><div class="range" id="detailRain24Range">—</div></div><div class="reading"><span>อุณหภูมิ</span><b id="detailTemp">—</b><div class="range" id="detailTempRange">—</div></div></div><div class="qualitybox"><div class="qualityrow"><b>ความเชื่อมั่นของข้อมูล</b><span class="pill" id="confidencePill">—</span></div><p class="source" id="sourceSummary">—</p><p class="explain" id="detailExplain"></p></div></section></aside>
<section class="card chartcard"><div class="sectionhead"><h2 id="chartTitle">แนวโน้ม 48 ชั่วโมงข้างหน้า</h2><span class="muted" style="font-size:.78rem">เส้นพยากรณ์ · จุดกลมคือค่าประเมินล่าสุด</span></div><svg id="chart" class="chart" viewBox="0 0 1200 240" aria-label="แนวโน้มพยากรณ์ 48 ชั่วโมง"></svg></section>
<section class="card sites"><div class="sectionhead"><h2>ทุกจุดในอุทยาน</h2><span class="muted" style="font-size:.78rem">ชื่อสถานที่เป็นชื่อหลัก รหัสเก็บไว้ในรายละเอียดระบบ</span></div><div id="siteGrid" class="sitegrid"></div></section>
<details class="card advanced"><summary>รายละเอียดวิธีคำนวณและแหล่งข้อมูล</summary><div class="advancedbody"><div><h3>ค่าที่แสดงมาจากอะไร</h3><ul><li>ฝน 3 ชั่วโมง: ผสมสถานีภาคพื้นดินใกล้เคียง แบบจำลอง และเรดาร์/ดาวเทียมเมื่อข้อมูลสด</li><li>ฝน 24 ชั่วโมง: รวมข้อมูลฝนรายชั่วโมงจากสถานี ThaiWater ที่มีอย่างน้อย 18/24 ชั่วโมง แล้วประมาณเชิงพื้นที่ ไม่ใช้พยากรณ์ย้อนหลังแทนค่าฝนจริง</li><li>พิกัด มป.8 และ มป.12 เป็นจุดอ้างอิงโดยประมาณสำหรับสกัดค่าพื้นที่ ไม่ใช่พิกัดตัวอาคารหรือกระบอกวัด</li><li>ระบบคำนวณใหม่ในแต่ละรอบข้อมูล โดยเป้าหมายการทำงานคือทุก 3 ชั่วโมง</li></ul></div><div><h3>การอ่านความแม่นยำ</h3><ul><li>คะแนนความเชื่อมั่นสะท้อนชนิดแหล่งข้อมูล ความใกล้สถานี ความครบ 24 ชั่วโมง และความพร้อมของพิกัด ไม่ใช่เปอร์เซ็นต์ความแม่นยำ</li><li>Error จริงจะรายงานเมื่อมีคู่ค่าคาดการณ์–ค่าตรวจวัดเพียงพอ: ฝนใช้ MAE/WAPE/CSI และอุณหภูมิใช้ MAE/RMSE</li><li>ช่วงค่าต่ำ–สูงในหน้าแรกคือความไม่แน่นอนเบื้องต้นจากความต่างระหว่างแหล่งข้อมูล</li></ul></div></div><p class="source-links">แหล่งข้อมูล: <a href="https://www.thaiwater.net/">ThaiWater</a> · <a href="https://weather.tmd.go.th/composite/compositeQPE_VTBB_latest.asc.zip">TMD Radar QPE</a> · <a href="https://gpm.nasa.gov/data/imerg">NASA GPM IMERG (เตรียมเชื่อม)</a> · <a href="https://open-meteo.com/en/docs">Open-Meteo</a></p></details>
</section><div class="foot" id="footerTime">—</div></main>
<script>
const DATA=__DASHBOARD_DATA__;
const $=id=>document.getElementById(id);
const reporting=DATA.locations.filter(d=>d.group==='reporting');
const located=reporting.filter(d=>d.latitude!==null&&d.longitude!==null);
const estimate=(id,v)=>DATA.site_estimates.find(d=>d.location_id===id&&d.variable===v)||null;
const rain24Status=DATA.rainfall_24h_status||{status:DATA.rainfall_24h.length?'fresh':'no_data',latest_hourly_at:null,age_hours:null,max_age_hours:6};
const rain24=(id)=>rain24Status.status==='fresh'?(DATA.rainfall_24h.find(d=>d.location_id===id)||null):null;
const forecast=(id,v)=>DATA.forecasts.filter(d=>d.location_id===id&&d.variable===v).slice(0,48);
const state={variable:'precipitation',selected:(reporting.find(d=>d.location_id==='HQ')||reporting[0]||{}).location_id};
const activeEstimate=(id)=>state.variable==='precipitation_24h'?rain24(id):estimate(id,state.variable);
function fmt(v,n=1){return v===null||v===undefined?'—':Number(v).toFixed(n)}
function levelText(level){return level==='high'?'สูง':level==='medium'?'ปานกลาง':'ต่ำ'}
function valueText(row){if(!row)return '—';return `${fmt(row.value)} ${row.unit}`}
function rangeText(row){if(!row)return 'ยังไม่มีค่าประเมิน';return `ช่วง ${fmt(row.uncertainty_low)}–${fmt(row.uncertainty_high)} ${row.unit}`}
function rain24StatusText(){if(rain24Status.status==='stale'){const when=rain24Status.latest_hourly_at?` · ล่าสุด ${clock.format(new Date(rain24Status.latest_hourly_at))}`:'';return `ข้อมูลฝนล่าช้า${when} — หยุดแสดงค่าที่เก่าเกิน ${rain24Status.max_age_hours||6} ชม.`}if(rain24Status.status==='no_data')return 'ยังไม่มีข้อมูลฝนรายชั่วโมง';return 'กำลังสะสมข้อมูลรายชั่วโมงให้ครบอย่างน้อย 18/24 ชม.'}
function rings(g){if(g.type==='Polygon')return [g.coordinates[0]];return g.coordinates.map(p=>p[0])}
const coords=rings(DATA.boundary).flat().concat(located.map(d=>[d.longitude,d.latitude]));
const xs=coords.map(c=>c[0]),ys=coords.map(c=>c[1]);const bounds={minX:Math.min(...xs),maxX:Math.max(...xs),minY:Math.min(...ys),maxY:Math.max(...ys)};
function project(c){const pad=36,w=760-pad*2,h=520-pad*2;return [pad+(c[0]-bounds.minX)/(bounds.maxX-bounds.minX)*w,pad+(bounds.maxY-c[1])/(bounds.maxY-bounds.minY)*h]}
function path(g){return rings(g).map(r=>r.map((c,i)=>`${i?'L':'M'}${project(c).join(',')}`).join(' ')+' Z').join(' ')}
function color(row){if(!row)return '#9aa59e';const v=row.value;if(state.variable==='temperature')return v>=34?'#a4443b':v>=31?'#d7673f':v>=28?'#e6a04d':'#3a8aaa';return v>=15?'#60439a':v>=5?'#176ea0':v>=1?'#43a0ba':v>0?'#9bc8d2':'#87a08e'}
const labelOffsets={HQ:[-152,-18],MP01:[12,-34],MP02:[12,-31],MP03:[12,17],MP04:[12,-32],MP05:[12,-34],MP06:[12,14],MP07:[-91,-34],MP08:[-91,16],MP09:[12,15],MP10:[12,14],MP11:[12,14],MP12:[12,-54]};
function shortName(d){if(d.location_id==='HQ')return 'ที่ทำการอุทยานฯ';return d.name_th}
function renderMap(){const svg=$('map'),ns='http://www.w3.org/2000/svg',parkD=path(DATA.boundary),midLat=(bounds.minY+bounds.maxY)/2,tenKmLon=10/(111.32*Math.cos(midLat*Math.PI/180)),scalePx=Math.abs(project([bounds.minX+tenKmLon,midLat])[0]-project([bounds.minX,midLat])[0]);svg.innerHTML=`<defs><linearGradient id="parkFill" x1="0" y1="0" x2="1" y2="1"><stop offset="0" stop-color="#eaf4e3"/><stop offset=".48" stop-color="#c4dbba"/><stop offset="1" stop-color="#8eb79b"/></linearGradient><pattern id="terrainPattern" width="86" height="48" patternUnits="userSpaceOnUse"><path d="M-18 34 Q12 4 43 26 T103 17" fill="none" stroke="#557b6540" stroke-width="1"/><path d="M-25 45 Q10 18 48 39 T112 30" fill="none" stroke="#ffffff55" stroke-width="1"/></pattern><filter id="mapShadow" x="-30%" y="-30%" width="160%" height="170%"><feDropShadow dx="0" dy="6" stdDeviation="7" flood-color="#173c2d" flood-opacity=".28"/></filter><filter id="markerShadow" x="-80%" y="-80%" width="260%" height="260%"><feDropShadow dx="0" dy="2" stdDeviation="2" flood-color="#173c2d" flood-opacity=".38"/></filter><filter id="markerShadowStrong" x="-100%" y="-100%" width="300%" height="300%"><feDropShadow dx="0" dy="3" stdDeviation="3" flood-color="#173c2d" flood-opacity=".55"/></filter><filter id="labelShadow" x="-20%" y="-40%" width="140%" height="180%"><feDropShadow dx="0" dy="1" stdDeviation="1.5" flood-color="#173c2d" flood-opacity=".18"/></filter><clipPath id="parkClipV2"><path d="${parkD}"/></clipPath></defs><g class="mapgrain">${[100,200,300,400,500,600,700].map(x=>`<line x1="${x}" y1="0" x2="${x}" y2="520" class="mapgrid"/>`).join('')}${[80,160,240,320,400,480].map(y=>`<line x1="0" y1="${y}" x2="760" y2="${y}" class="mapgrid"/>`).join('')}</g><path d="${parkD}" class="parkshadow"/><path d="${parkD}" class="park"/><path d="${parkD}" class="parkinner"/><g transform="translate(710 40)"><text x="0" y="-12" text-anchor="middle" class="compasstext">N</text><path d="M0,-7 L-7,11 L0,7 L7,11 Z" class="compass"/><circle cx="0" cy="2" r="16" fill="none" stroke="#355f4d66"/></g><g transform="translate(52 476)"><line x1="0" y1="0" x2="${scalePx.toFixed(1)}" y2="0" class="scaleline"/><line x1="0" y1="-4" x2="0" y2="4" class="scaleline"/><line x1="${scalePx.toFixed(1)}" y1="-4" x2="${scalePx.toFixed(1)}" y2="4" class="scaleline"/><text x="${(scalePx/2).toFixed(1)}" y="-7" text-anchor="middle" class="scaletext">10 กม.</text></g>`;const values=located.map(d=>activeEstimate(d.location_id)?.value).filter(v=>v!==undefined),maxValue=Math.max(...values,1),placed=[];located.forEach((d,index)=>{const row=activeEstimate(d.location_id),[x,y]=project([d.longitude,d.latitude]),ratio=row?Math.max(0,Math.min(1,row.value/maxValue)):0,r=6+ratio*5+(state.selected===d.location_id?2:0),g=document.createElementNS(ns,'g'),halo=document.createElementNS(ns,'circle'),c=document.createElementNS(ns,'circle');halo.setAttribute('cx',x);halo.setAttribute('cy',y);halo.setAttribute('r',r+3);halo.setAttribute('class','markerhalo');g.appendChild(halo);if(state.selected===d.location_id){const ring=document.createElementNS(ns,'circle');ring.setAttribute('cx',x);ring.setAttribute('cy',y);ring.setAttribute('r',r+7);ring.setAttribute('class','selectedring');g.appendChild(ring)}c.setAttribute('cx',x);c.setAttribute('cy',y);c.setAttribute('r',r);c.setAttribute('fill',color(row));c.setAttribute('class',`marker ${state.selected===d.location_id?'selected':''}`);c.setAttribute('tabindex','0');c.setAttribute('role','button');c.setAttribute('aria-label',`${d.name_th} ${valueText(row)}`);c.onclick=()=>selectSite(d.location_id);c.onkeydown=e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();selectSite(d.location_id)}};const title=document.createElementNS(ns,'title');title.textContent=`${d.name_th} · ${valueText(row)} · ความเชื่อมั่น${levelText(row?.confidence_level||'low')}${d.confidence==='low'?' · พิกัดอ้างอิงโดยประมาณ':''}`;c.appendChild(title);g.appendChild(c);const name=shortName(d),value=valueText(row),boxW=Math.min(150,Math.max(67,name.length*7.1+16)),preferred=labelOffsets[d.location_id]||(x>540?[-boxW-13,index%2?10:-30]:[12,index%2?10:-30]),candidates=[preferred,[12,-35],[12,10],[-boxW-12,-35],[-boxW-12,10],[12,-72],[-boxW-12,-72],[12,48],[-boxW-12,48]],overlap=q=>placed.some(p=>q.x<p.x+p.w+4&&q.x+q.w+4>p.x&&q.y<p.y+p.h+4&&q.y+q.h+4>p.y),valid=q=>q.x>=8&&q.x+q.w<=752&&q.y>=8&&q.y+q.h<=500;let off=candidates.find(candidate=>{const q={x:x+candidate[0],y:y+candidate[1],w:boxW,h:31};return valid(q)&&!overlap(q)})||preferred,lx=Math.max(8,Math.min(752-boxW,x+off[0])),ly=Math.max(8,Math.min(489,y+off[1]));placed.push({x:lx,y:ly,w:boxW,h:31});const leader=document.createElementNS(ns,'line');leader.setAttribute('x1',x);leader.setAttribute('y1',y);leader.setAttribute('x2',lx+(lx<x?boxW:0));leader.setAttribute('y2',ly+13);leader.setAttribute('stroke','#476b5a88');leader.setAttribute('stroke-width','1');g.appendChild(leader);const box=document.createElementNS(ns,'rect');box.setAttribute('x',lx);box.setAttribute('y',ly);box.setAttribute('width',boxW);box.setAttribute('height','31');box.setAttribute('rx','7');box.setAttribute('class','maplabelbox');g.appendChild(box);const t=document.createElementNS(ns,'text');t.setAttribute('x',lx+8);t.setAttribute('y',ly+12);t.setAttribute('class','maplabel');t.textContent=name;g.appendChild(t);const vt=document.createElementNS(ns,'text');vt.setAttribute('x',lx+8);vt.setAttribute('y',ly+24);vt.setAttribute('class','mapvalue');vt.textContent=value;g.appendChild(vt);svg.appendChild(g)});$('legendMetric').textContent=state.variable==='temperature'?'อุณหภูมิ':state.variable==='precipitation_24h'?'ฝนสะสม 24 ชม.':'ฝนสะสม 3 ชม.';$('legendBar').style.background=state.variable==='temperature'?'linear-gradient(90deg,#3a8aaa,#9bc7bd,#e6a04d,#d7673f,#a4443b)':'linear-gradient(90deg,#8ba58f,#84cad5,#36a1bd,#176ea0,#60439a)'}
function renderDetail(){const d=reporting.find(x=>x.location_id===state.selected);if(!d)return;const rain=estimate(d.location_id,'precipitation'),r24=rain24(d.location_id),temp=estimate(d.location_id,'temperature'),active=activeEstimate(d.location_id),fallback=active||rain||temp,isMissingRain24=state.variable==='precipitation_24h'&&!r24;$('detailName').textContent=d.name_th;const approximate=d.verification_status==='unverified_spatial_approximation';$('detailKind').textContent=isMissingRain24?'ยังไม่แสดงค่าฝนสะสม 24 ชั่วโมง':fallback?.spatial_basis==='park_regional'?'ค่าประเมินระดับพื้นที่ · ยังไม่มีพิกัดยืนยัน':approximate?'จุดอ้างอิงโดยประมาณ · รอยืนยันภาคสนาม':'ค่าประเมิน ณ จุดรายงาน';const activeTime=active?.window_end||active?.estimate_at;$('detailTime').textContent=isMissingRain24?rain24StatusText():activeTime?`รอบข้อมูล ${new Date(activeTime).toLocaleString('th-TH')}`:'ยังไม่มีค่าประเมิน';$('detailRain').textContent=valueText(rain);$('detailRainRange').textContent=rangeText(rain);$('detailRain24').textContent=valueText(r24);$('detailRain24Range').textContent=r24?`${rangeText(r24)} · ข้อมูล ${fmt(r24.coverage_hours,0)}/24 ชม.`:rain24StatusText();$('detailTemp').textContent=valueText(temp);$('detailTempRange').textContent=rangeText(temp);const confidenceRow=isMissingRain24?null:fallback,level=confidenceRow?.confidence_level||'low';$('confidencePill').className=`pill ${level}`;$('confidencePill').textContent=confidenceRow?`${levelText(level)} · ${fmt(confidenceRow.confidence_score,0)}/100`:'ยังประเมินไม่ได้';$('sourceSummary').textContent=isMissingRain24?'ที่มา: ThaiWater · รอข้อมูลฝนรายชั่วโมงรอบใหม่':fallback?`ที่มา: ${fallback.source_summary}`:'ยังไม่มีแหล่งข้อมูล';$('detailExplain').textContent=isMissingRain24?'ระบบไม่นำยอดสะสมเก่าหรือค่าพยากรณ์ย้อนหลังมาแสดงแทนค่าฝนจริง':approximate?'พิกัดนี้ใช้สกัดค่าพื้นที่เท่านั้น ไม่ใช่ตำแหน่งตัวอาคารหรือกระบอกวัด จึงจำกัดคะแนนความเชื่อมั่นไว้ต่ำ':fallback?.spatial_basis==='park_regional'?'ค่านี้เป็นตัวแทนภาพรวมอุทยานและมีช่วงกว้าง ไม่ใช่ค่าตรวจวัดตรงสถานที่ จนกว่าจะยืนยันพิกัดได้':fallback?.validation_status==='awaiting_validation'?'กำลังสะสมข้อมูลย้อนหลังเพื่อคำนวณ error จริง ค่าปัจจุบันจึงแสดงเป็นช่วงความไม่แน่นอน':''}
function renderChart(){const d=reporting.find(x=>x.location_id===state.selected),variable=state.variable==='temperature'?'temperature':'precipitation',rows=forecast(state.selected,variable),current=estimate(state.selected,variable),svg=$('chart');$('chartTitle').textContent=`${variable==='precipitation'?'ฝนรายชั่วโมง':'อุณหภูมิ'} 48 ชั่วโมง · ${d?.name_th||''}`;if(!rows.length){svg.innerHTML='<text x="600" y="122" text-anchor="middle" class="charttext">ยังไม่มีพยากรณ์เฉพาะจุด — ใช้ค่าประเมินพื้นที่ล่าสุดด้านบน</text>';return}const pts=rows.map(r=>({t:Date.parse(r.valid_at),v:r.value}));const W=1200,H=240,p={l:55,r:18,t:18,b:32},minT=Math.min(...pts.map(x=>x.t)),maxT=Math.max(...pts.map(x=>x.t)),minV=Math.min(...pts.map(x=>x.v),current?.uncertainty_low??Infinity),maxV=Math.max(...pts.map(x=>x.v),current?.uncertainty_high??-Infinity),spanV=maxV-minV||1,spanT=maxT-minT||1,x=t=>p.l+(t-minT)/spanT*(W-p.l-p.r),y=v=>p.t+(maxV-v)/spanV*(H-p.t-p.b);let html='';for(let i=0;i<4;i++){const yy=p.t+i*(H-p.t-p.b)/3,val=maxV-i*spanV/3;html+=`<line x1="${p.l}" y1="${yy}" x2="${W-p.r}" y2="${yy}" class="gridline"/><text x="${p.l-8}" y="${yy+4}" text-anchor="end" class="charttext">${fmt(val)}</text>`}html+=`<path d="${pts.map((q,i)=>`${i?'L':'M'}${x(q.t).toFixed(1)},${y(q.v).toFixed(1)}`).join(' ')}" class="forecastline"/><line x1="${p.l}" y1="${H-p.b}" x2="${W-p.r}" y2="${H-p.b}" class="axis"/>`;if(current)html+=`<circle cx="${x(minT)}" cy="${y(current.value)}" r="5" class="point"/>`;html+=`<text x="${p.l}" y="${H-9}" class="charttext">${new Date(minT).toLocaleString('th-TH',{day:'numeric',month:'short',hour:'2-digit'})}</text><text x="${W-p.r}" y="${H-9}" text-anchor="end" class="charttext">${new Date(maxT).toLocaleString('th-TH',{day:'numeric',month:'short',hour:'2-digit'})}</text>`;svg.innerHTML=html}
function renderSites(){$('siteGrid').innerHTML=reporting.map(d=>{const rain=estimate(d.location_id,'precipitation'),r24=rain24(d.location_id),temp=estimate(d.location_id,'temperature'),active=activeEstimate(d.location_id),approximate=d.verification_status==='unverified_spatial_approximation',missingRain24=state.variable==='precipitation_24h'&&!r24,hint=missingRain24?'ข้อมูลฝนล่าช้า · งดแสดงค่าที่หมดอายุ':approximate?'พิกัดอ้างอิงโดยประมาณ · ความเชื่อมั่นต่ำ':active?.spatial_basis==='park_regional'?'ค่าระดับพื้นที่ · พิกัดรอยืนยัน':`ความเชื่อมั่น${levelText(active?.confidence_level||'low')}`;return `<button class="siteitem ${state.selected===d.location_id?'selected':''}" onclick="selectSite('${d.location_id}')"><div class="sitename">${d.name_th}</div><div class="sitevals"><span>3 ชม. ${rain?fmt(rain.value)+' mm':'—'}</span><span>24 ชม. ${r24?fmt(r24.value)+' mm':rain24Status.status==='stale'?'ข้อมูลล่าช้า':'—'}</span><span>${temp?fmt(temp.value)+' °C':'—'}</span></div><div class="sitehint">${hint}</div></button>`}).join('')}
function renderKpis(){const covered=reporting.filter(d=>estimate(d.location_id,'precipitation')&&estimate(d.location_id,'temperature')&&rain24(d.location_id)).length,rains=reporting.map(d=>estimate(d.location_id,'precipitation')?.value).filter(v=>v!==undefined),rains24=reporting.map(d=>rain24(d.location_id)?.value).filter(v=>v!==undefined),temps=reporting.map(d=>estimate(d.location_id,'temperature')?.value).filter(v=>v!==undefined),trusted=reporting.filter(d=>{const r=estimate(d.location_id,'precipitation'),t=estimate(d.location_id,'temperature'),r24=rain24(d.location_id);return r&&t&&r24&&r.confidence_score>=55&&t.confidence_score>=55&&r24.confidence_score>=55}).length;$('kpiCoverage').textContent=`${covered}/${reporting.length} จุด`;$('kpiRain').textContent=rains.length?`${fmt(Math.max(...rains))} mm`:'—';$('kpiRain24').textContent=rains24.length?`${fmt(Math.max(...rains24))} mm`:rain24Status.status==='stale'?'ข้อมูลล่าช้า':'—';$('kpiTemp').textContent=temps.length?`${fmt(Math.min(...temps))}–${fmt(Math.max(...temps))} °C`:'—';$('kpiConfidence').textContent=`${trusted}/${reporting.length} จุด`}
function selectSite(id){state.selected=id;$('siteSelect').value=id;renderMap();renderDetail();renderChart();renderSites()}
function setVariable(v){state.variable=v;$('rainBtn').classList.toggle('active',v==='precipitation');$('rain24Btn').classList.toggle('active',v==='precipitation_24h');$('tempBtn').classList.toggle('active',v==='temperature');renderFreshness();renderMap();renderDetail();renderChart();renderSites()}
const latest=DATA.site_estimates.map(d=>Date.parse(d.estimate_at)).concat(DATA.rainfall_24h.map(d=>Date.parse(d.window_end))).filter(Number.isFinite),generatedTime=new Date(DATA.generated_at),latestTime=latest.length?new Date(Math.max(...latest)):generatedTime,nextUpdate=new Date(generatedTime.getTime()+3*36e5),now=new Date(),ageHours=Math.max(0,(now-latestTime)/36e5),clock=new Intl.DateTimeFormat('th-TH',{dateStyle:'short',timeStyle:'short',timeZone:'Asia/Bangkok'});
function renderFreshness(){if(state.variable==='precipitation_24h'&&rain24Status.status!=='fresh'){$('freshBadge').className='fresh stale';$('freshBadge').textContent=rain24Status.status==='stale'?`ข้อมูลฝนล่าช้า · ${rain24Status.latest_hourly_at?clock.format(new Date(rain24Status.latest_hourly_at)):'ไม่ทราบเวลา'}`:'ยังไม่มีข้อมูลฝนรายชั่วโมง';$('updateLine').textContent=rain24StatusText();return}const freshState=ageHours<=4?'fresh':ageHours<=8?'warning':'stale',freshLabel=freshState==='fresh'?'ข้อมูลล่าสุด':freshState==='warning'?'ข้อมูลเริ่มล่าช้า':'ข้อมูลเก่า';$('freshBadge').className=`fresh ${freshState==='fresh'?'':freshState}`;$('freshBadge').textContent=`${freshLabel} · ${clock.format(latestTime)}`;$('updateLine').textContent=nextUpdate>now?`รอบถัดไปภายใน ${clock.format(nextUpdate)} · ทุก 3 ชั่วโมง`:`เลยรอบที่คาดไว้ ${clock.format(nextUpdate)} · ระบบกำลังรออัปเดต`}
$('rainBtn').onclick=()=>setVariable('precipitation');$('rain24Btn').onclick=()=>setVariable('precipitation_24h');$('tempBtn').onclick=()=>setVariable('temperature');$('siteSelect').innerHTML=reporting.map(d=>`<option value="${d.location_id}">${d.name_th}</option>`).join('');$('siteSelect').value=state.selected;$('siteSelect').onchange=e=>selectSite(e.target.value);$('generatedBadge').textContent=`สร้างหน้าเว็บ ${clock.format(generatedTime)}`;$('footerTime').textContent=`ข้อมูลล่าสุด ${clock.format(latestTime)} · สร้างหน้า Dashboard ${clock.format(generatedTime)} · วิธีคำนวณ maeping_blend_v1 / maeping_rain24_idw_v1`;renderFreshness();renderKpis();renderMap();renderDetail();renderChart();renderSites();
</script></body></html>'''
